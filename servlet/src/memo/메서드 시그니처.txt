이번 시간에는 RequestMappingAdapterMapping 인터페이스가 제공하는 무수한 서로 다른 형태의 반환형, 매개변수를 처리하는 Adapter를 활용하여 우리 입맞에 맞게 v2의 메서드 시그니처를
변경해 볼 것이다. 이 방식이 실무에서 주로 사용이 된다.
그러나 이 버전에도 문제점이 있다.
그건 바로 HTTP METHOD를 구분하지 않는다는 점이다. HTTP Request가 GET이든 POST든 PUT이던 전부 수용해 버린다. 이건 좋은 방식은 아니다.
